# Canonical repo

This tree now lives in **https://github.com/T00few2/dzr** under `apps/api`.

Do not add features here. Cloud Run should be retargeted to `T00few2/dzr` with source directory `apps/api` after that repo is pushed.
